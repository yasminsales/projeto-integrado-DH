const {resolve} = require('path'); //modulo de dentro do node, por isso usamos esse modelo tradicional e não o import

module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.js'), //diretórios para executar os comandos de cli
    'models-path': resolve(__dirname, 'src', 'app', 'models'),
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
};
